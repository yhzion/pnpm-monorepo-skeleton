{
  "name": "@mmlp/core",
  "version": "0.1.0",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "lint": "eslint --fix",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@mmlp/core": "workspace:*"
  },
  "devDependencies": {}
}
